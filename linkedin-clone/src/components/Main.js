import styled from "styled-components";

const Main = (props) => {
  return (
    <Container>
      Main
    </Container>
  )
}

export default Main

const Container = styled.div`
  grid-area: Main;
`